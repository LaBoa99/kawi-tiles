import { ToolListnerDirective } from './tool-listner.directive';

describe('ToolListnerDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolListnerDirective();
    expect(directive).toBeTruthy();
  });
});
